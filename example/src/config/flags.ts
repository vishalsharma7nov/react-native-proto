/**
 * Flip this to false to call the real API in react-native-proto.config.ts.
 */
export const USE_MOCK_SERVER = true;

export const DEMO_USER_ID = "1";

export const MOCK_USER = {
  name: "Ada Lovelace",
  age: 36,
} as const;
