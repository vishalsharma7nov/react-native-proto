/**
 * Expo entry — mounts App.tsx.
 *
 * For the proto demo, start here:
 *   - react-native-proto.config.ts
 *   - src/api/client.ts
 *   - src/screens/HomeScreen.tsx
 */
import { registerRootComponent } from "expo";
import App from "./App";

registerRootComponent(App);
